import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'

// Kode ini yang nge-render seluruh aplikasi lo
ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
